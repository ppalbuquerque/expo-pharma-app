import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    borderColor: "#DDDDDD",
    borderWidth: 1,
    borderRadius: 12,
    padding: 16,
    flexDirection: "row",
  },
  searchIcon: {
    marginRight: 12,
  },
});
